'use strict';

describe('myApp.employeeList module', function() {

  beforeEach(module('myApp.employeeList'));

  describe('employeeList controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});